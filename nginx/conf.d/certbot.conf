server {
    listen 80;
    server_name gather.quentintheet.fr api.gather.quentintheet.fr;

    # dossier pour les challenges let's encrypt
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Temporairement, on ne redirige PAS vers HTTPS pour permettre la validation Certbot
    # La redirection sera activée dans les configs backend/frontend HTTPS
    location / {
        return 200 "Certbot validation in progress";
        add_header Content-Type text/plain;
    }
}
